class Player {

    constructor(){
        this.ships = [];
        this.battleField = {};
    }

    setShip(ship) {
        this.ships.push(ship);
    }

    shoot(index){

    }

}

export default Player;
